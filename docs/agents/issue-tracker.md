# Issue Tracker

**미정 (아직 결정하지 않음).**

이 저장소에서 이슈를 어디서 관리할지 아직 정하지 않았습니다. `to-tickets`, `triage`, `to-spec` 등 이슈 트래커에 읽고·쓰는 스킬을 처음 사용하기 전에 아래 중 하나를 골라 이 파일을 채워주세요.

## 후보

- **Local markdown** — 이슈를 이 저장소의 `.scratch/<feature>/` 아래 마크다운 파일로 관리 (원격/`gh` 불필요, 솔로 개발에 적합)
- **GitHub Issues** — `gh` CLI 사용. 현재 원격은 `github.com/0w0i0n0g0/cgv-open-push` (소유자 다름, `gh` 미설치)
- **GitLab Issues** — `glab` CLI 사용
- **기타 (Jira/Linear 등)** — 워크플로우를 산문으로 여기에 기술

결정하면 `/setup-matt-pocock-skills`를 다시 실행하거나, 이 파일을 직접 편집하세요.
